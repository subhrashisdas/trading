import { Candle } from './candle';

export async function runAlgoOfACandle(candle: Candle, algoName: string, type: 'trade' | 'squareoff') {}

export async function runAlgoForADay(candle: Candle, algoName: string) {}
