import { Candle } from '@src/candle';
import { Milliseconds } from '@src/date';

export async function runAlgoOfACandle(candle: Candle, algoName: string, type: 'trade' | 'squareoff') {}

export async function runAlgoForADay(dateTime: Milliseconds, algoName: string) {}
